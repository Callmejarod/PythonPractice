#For the users to input their answers

first = input('First: ')
second = input('Second: ')
#Instead of using int, use float for decimal point numbers

sum = float(first) + float(second)
#Use the str to convert and print the input back into a string varible

print('Sum:' + str(sum))
#This is a comment